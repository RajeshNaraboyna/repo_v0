apiVersion: v1
kind: Service
metadata:
  name: {{ include "scl-api.fullname" . }}
  labels:
    {{- include "scl-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scl-api.selectorLabels" . | nindent 4 }}
