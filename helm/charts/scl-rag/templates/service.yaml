apiVersion: v1
kind: Service
metadata:
  name: {{ include "scl-rag.fullname" . }}
  labels:
    {{- include "scl-rag.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scl-rag.selectorLabels" . | nindent 4 }}
